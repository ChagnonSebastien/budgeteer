import { default as styled } from 'styled-components'

/**
 * Standard form container used across all CRUD pages.
 * Eliminates duplication of the same styled component in 8+ pages.
 */
export const CrudFormContainer = styled.div`
  width: 100%;
  max-width: 50rem;
  margin: auto;
`
